name: Release checks
on:
  push:

jobs:
    demo-job:
        name: Placeholder for release checks script
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Just echo
              run: |     
                echo "Placeholder for release checks script!"
